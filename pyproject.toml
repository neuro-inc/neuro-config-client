[build-system]
requires = ["setuptools>=82.0.1", "wheel>=0.47.0", "setuptools_scm>=10.0.5"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]

[tool.black]
exclude = '''
/(
      \.git
    | venv
    | __pycache__
    | \.tox
)/
'''
